/*
Package symtab implements a multi-level symbol table. 

New scopes can be pushed onto the symbol table to enable shadowing
of symbols in inner scopes, and lookups of symbols in outer scopes.
*/
package symtab
